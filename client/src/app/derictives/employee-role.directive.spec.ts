import { EmployeeRoleDirective } from './employee-role.directive';

describe('EmployeeRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeRoleDirective();
    expect(directive).toBeTruthy();
  });
});
